// Site-wide constants available to every template as `site`.
// `url` is the canonical production origin (no trailing slash) — used for
// absolute URLs in the sitemap, canonical links, and social/OG tags.
module.exports = {
  url: "https://danylokravchenko.github.io/rusty-amp",
};
